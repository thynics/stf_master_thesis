\chapter{Results}
\label{chap:results}

This chapter evaluates the CUDASTF runtime scheduler on a single-node
multi-GPU platform. It first defines the experimental platform and metrics,
then states the evaluation plan, reports the headline results, and finally
explains the main placement and DVFS outcomes in more detail.

\section{Experimental Setup and Metrics}
\label{sec:results-setup}

All experiments run on a single Slurm-managed node with eight NVIDIA L4 GPUs.
The application-visible GPU set contains all eight GPUs, and power sampling is
also performed across the same eight devices. The code is built in Release mode
inside an Apptainer container based on CUDA 12.4.1. The CUDA compiler is NVIDIA
12.4.131, the host compiler is GCC 11.4, and CUDA architecture 89 is used for
the generated device code.

The evaluation uses six CUDASTF benchmarks: FDTD, CG, MiniWeather, Cholesky,
LU decomposition, and Blocked N-body. These benchmarks cover both
copy-sensitive task graphs and near-neutral task graphs, which is important for
evaluating whether a residual scheduler improves favorable cases without
forcing changes where there is little opportunity.

Kernel latency is the primary performance metric. For DVFS, kernel energy is
the primary energy metric. Kernel energy is computed by sampling NVML power on
all visible GPUs and trapezoid-integrating the samples over the benchmark
kernel window. Kernel power is reported as supporting evidence for the energy
result. Whole-process energy is treated as auxiliary context and is not used
for the headline DVFS claim.

All relative results use the baseline named in the corresponding section.
Negative deltas in latency, energy, power, and copy traffic indicate reductions
relative to that baseline.

\section{Evaluation Plan}
\label{sec:results-plan}

The evaluation separates placement from frequency control. The placement
experiments compare HEFT with the bandit scheduler while DVFS is disabled. The
data-movement analysis then checks whether the latency reductions align with
copy-volume reductions and accepted non-HEFT placements. The DVFS experiments
hold the placement layer fixed and compare WinDVFS against a no-DVFS baseline
using kernel energy as the main metric.

This organization keeps the two contributions distinct. Placement results
answer whether the scheduler improves task placement and locality. DVFS results
answer whether guarded frequency control can reduce kernel energy with a small
latency cost.

\section{Key Results}
\label{sec:results-key}

Table~\ref{tab:results-headline} summarizes the main results. The bandit
scheduler reduces geometric-mean kernel latency by \(7.146\%\) relative to
HEFT. WinDVFS reduces geometric-mean kernel energy by \(2.904\%\) with a
\(0.626\%\) geometric-mean kernel-latency overhead.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lr@{}}
\toprule
Metric & Value \\
\midrule
Placement geometric-mean kernel-latency reduction & \(7.146\%\) \\
Placement geometric-mean speedup & \(1.076965\times\) \\
Largest placement latency reduction & \(18.539\%\) \\
DVFS geometric-mean kernel-energy reduction & \(2.904\%\) \\
DVFS geometric-mean kernel-latency overhead & \(0.626\%\) \\
Largest single-benchmark DVFS energy reduction & \(5.587\%\) \\
\bottomrule
\end{tabular}
\caption{Headline results across the benchmark suite. Placement is compared with HEFT; DVFS is compared with no-DVFS execution.}
\label{tab:results-headline}
\end{table}

\section{Placement Results}
\label{sec:results-placement-performance}

Table~\ref{tab:results-placement-main} reports the per-benchmark placement
results. The strongest improvements appear on CG, Cholesky, and LU
decomposition. MiniWeather and Blocked N-body remain near the HEFT baseline,
which is the desired behavior for workloads with little useful placement
freedom. FDTD improves in the aggregate but is less stable than the
linear-algebra workloads.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lrr@{}}
\toprule
Benchmark & Kernel latency \(\Delta\) & Kernel energy \(\Delta\) \\
\midrule
FDTD & \(-1.333\%\) & \(-0.597\%\) \\
CG & \(-12.693\%\) & \(-12.316\%\) \\
MiniWeather & \(+0.019\%\) & \(-0.062\%\) \\
Cholesky & \(-8.638\%\) & \(-2.441\%\) \\
LU decomposition & \(-18.539\%\) & \(-17.872\%\) \\
Blocked N-body & \(-0.052\%\) & \(-0.580\%\) \\
\bottomrule
\end{tabular}
\caption{Placement-only results relative to HEFT. DVFS is disabled.}
\label{tab:results-placement-main}
\end{table}

The result is not a claim that every benchmark accelerates equally. Instead,
the placement layer behaves selectively. It extracts large gains when the task
graph exposes useful copy-saving alternatives, and it stays close to HEFT when
those alternatives are absent.

\section{Data-Movement Evidence}
\label{sec:results-locality-attribution}

Table~\ref{tab:results-locality-attribution} connects the placement result to
copy traffic. Cholesky and LU decomposition show the largest latency reductions
and also large copy-byte reductions. FDTD shows a smaller but still visible
copy reduction. MiniWeather and Blocked N-body show zero copy-byte reduction
and zero non-HEFT deviation rate, matching their near-neutral placement
results.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
Benchmark & Latency \(\Delta\) & Copy bytes \(\Delta\) & Deviation rate \\
\midrule
FDTD & \(-3.135\%\) & \(-12.030\%\) & 0.249902 \\
MiniWeather & \(-0.0066\%\) & \(0.000\%\) & 0.000000 \\
Cholesky & \(-9.269\%\) & \(-38.220\%\) & 0.281154 \\
LU decomposition & \(-15.891\%\) & \(-23.905\%\) & 0.619305 \\
Blocked N-body & \(+0.019\%\) & \(0.000\%\) & 0.000000 \\
\bottomrule
\end{tabular}
\caption{Data-movement evidence for the placement result. Negative latency and copy-byte deltas are reductions relative to HEFT.}
\label{tab:results-locality-attribution}
\end{table}

The locality data supports the design of the residual placement gate. When a
non-HEFT placement can reduce copy traffic while keeping the predicted local
penalty bounded, the scheduler accepts the deviation. When that opportunity is
not present, the scheduler naturally falls back to HEFT-like behavior.

The placement setting was also checked against nearby scheduler settings. The
selected bandit-scheduler setting remained the best aggregate choice in that
check, with a four-benchmark geometric-mean speedup of \(1.076371\times\)
relative to HEFT. This supports using it as the thesis placement setting.

\section{DVFS Energy Results}
\label{sec:results-dvfs-energy}

Table~\ref{tab:results-dvfs-energy} reports the final DVFS result. FDTD,
MiniWeather, CG, and LU decomposition reduce kernel energy by roughly
\(3\%\)--\(6\%\) with less than one percent latency overhead. Cholesky also
reduces kernel energy, but pays a larger latency cost. Blocked N-body remains
near neutral, which matches the placement evidence that this workload offers
little useful control opportunity.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
Benchmark & Kernel latency \(\Delta\) & Kernel energy \(\Delta\) & Kernel power \(\Delta\) \\
\midrule
FDTD & \(-0.604\%\) & \(-3.829\%\) & \(-3.230\%\) \\
MiniWeather & \(+0.073\%\) & \(-3.079\%\) & \(-3.150\%\) \\
CG & \(+0.953\%\) & \(-5.587\%\) & \(-6.478\%\) \\
LU decomposition & \(+0.662\%\) & \(-3.287\%\) & \(-3.968\%\) \\
Cholesky & \(+2.817\%\) & \(-1.411\%\) & \(-4.389\%\) \\
Blocked N-body & \(-0.110\%\) & \(-0.137\%\) & \(-0.023\%\) \\
\bottomrule
\end{tabular}
\caption{Final WinDVFS result relative to no-DVFS execution. Energy and power are kernel-window NVML metrics.}
\label{tab:results-dvfs-energy}
\end{table}

Across the six benchmarks, the geometric-mean kernel-energy ratio is
0.970962, corresponding to \(2.904\%\) lower normalized kernel energy. The
geometric-mean latency ratio is 1.006259, corresponding to a \(0.626\%\)
latency overhead. This is the main DVFS result: guarded window-level frequency
control reduces kernel energy on this benchmark set while keeping aggregate
latency cost below one percent.

\section{Clock and Power Evidence}
\label{sec:results-dvfs-clock-guardrail}

Table~\ref{tab:results-dvfs-clock} reports the clock and power data behind the
DVFS result. The larger energy reductions coincide with lower average kernel
power and visible exposure to the middle SM clock. The neutral Blocked N-body
row shows almost no movement in clock or power, so the controller does not
force an energy claim where the workload provides little opportunity.

\begin{table}[t]
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}lrrrr@{}}
\toprule
Benchmark & Base SM MHz & WinDVFS SM MHz & MID samples & Kernel energy \(\Delta\) \\
\midrule
FDTD & 1931.442 & 1852.530 & 91.903\% & \(-3.829\%\) \\
MiniWeather & 2040.000 & 2028.091 & 0.000\% & \(-3.079\%\) \\
CG & 2032.581 & 1876.795 & 98.912\% & \(-5.587\%\) \\
LU decomposition & 2040.000 & 1943.170 & 58.685\% & \(-3.287\%\) \\
Cholesky & 2040.000 & 1943.222 & 58.654\% & \(-1.411\%\) \\
Blocked N-body & 2040.000 & 2040.000 & 0.000\% & \(-0.137\%\) \\
\bottomrule
\end{tabular}%
}
\caption{Clock and energy data for the WinDVFS rows. MID samples report the fraction of kernel-window samples at the middle SM clock level.}
\label{tab:results-dvfs-clock}
\end{table}

The clock data also explains why the DVFS result is workload dependent. CG and
FDTD have high middle-clock exposure and clear energy reductions. LU
decomposition and Cholesky show partial middle-clock exposure; LU
decomposition keeps the latency cost small, while Cholesky exposes the
expected trade-off between lower power and longer runtime. Blocked N-body
remains close to the no-DVFS baseline.

\section{Overheads and Limitations}
\label{sec:results-overhead}

The diagnostic overhead experiment indicates that compact placement logging is
close to noise-scale for FDTD and Blocked N-body, while LU decomposition shows
a visible logging cost. Compact WinDVFS control is not a pure
logging-only measurement because it also includes policy behavior and frequency
changes. For this reason, final timing and energy claims rely on the placement
and DVFS rows above rather than on diagnostic rows.

The evaluation has three main limitations. First, all results are measured on a
single eight-GPU node, so the magnitude of the results may change with GPU
generation, interconnect topology, driver behavior, and benchmark scale.
Second, FDTD is noisier than the linear-algebra workloads and should be read as
a modest locality case rather than as the strongest placement example. Third,
the DVFS conclusion is based on kernel-window energy and latency; it does not
replace a full-system power study.

\section{Results Summary}
\label{sec:results-summary}

The experimental evidence supports both layers of the proposed design. The
bandit scheduler improves geometric-mean kernel latency by \(7.146\%\) relative
to HEFT, with the largest gains appearing on copy-sensitive workloads. The
data-movement results show that these gains align with reduced copy traffic,
while neutral workloads remain close to HEFT.

WinDVFS reduces geometric-mean kernel energy by \(2.904\%\) with \(0.626\%\)
geometric-mean latency overhead. The result is not a universal claim that every
benchmark benefits from DVFS. It is a controlled result showing that a guarded
window-level DVFS layer can extract energy savings from several CUDASTF
workloads while keeping aggregate latency cost small and leaving
low-opportunity workloads near the baseline.
