\chapter{Results}
\label{chap:results}

This chapter evaluates the proposed CUDASTF runtime scheduler using a set of
controlled placement and DVFS experiments. The evaluation is organized around
the research questions introduced in Chapter~\ref{chap:methods}: whether a
HEFT-relative placement policy can improve locality without sacrificing the
stability of HEFT, whether the observed speedups can be attributed to concrete
data-movement mechanisms, and whether a guarded window-level DVFS controller
can reduce energy while keeping latency overhead small.

The chapter deliberately separates placement evidence from DVFS evidence. In
the placement experiments, DVFS is disabled and the comparison is between HEFT
and \texttt{bandit\_scheduler}. In the DVFS experiments, the placement
framework is held fixed and the comparison is between a no-DVFS baseline and
\texttt{win\_dvfs}. This separation makes the results easier to interpret:
placement changes where a task executes, while DVFS changes the frequency at
which the selected GPU runs.

The main placement result is that \texttt{bandit\_scheduler} improves
geometric-mean kernel latency by \(7.146\%\) across six benchmarks when the
mean of each five-run benchmark batch is used. The strongest placement gains
occur on \texttt{cg}, \texttt{cholesky}, and \texttt{lu\_decomposition}, and
the locality analysis shows that these gains coincide with large reductions in
copy traffic. The main DVFS result is that \texttt{win\_dvfs} reduces
geometric-mean kernel energy by \(2.904\%\) with a geometric-mean kernel
latency overhead of \(0.626\%\). The DVFS effect is therefore useful but
workload dependent: four benchmarks show clear kernel-energy reductions, one
benchmark shows a smaller energy reduction with a visible latency trade-off,
and one benchmark remains near neutral.

\section{Experimental Protocol}
\label{sec:results-protocol}

All final experiments target single-node, eight-GPU CUDASTF execution. The
application-visible GPU set contains eight GPUs, and every row used in the
final tables sampled all eight allocated devices. Each benchmark row is
launched as a fresh process so that CUDA context state, scheduler state, and
DVFS state do not intentionally carry over between measured rows.

The evaluation uses six CUDASTF benchmarks: \texttt{fdtd}, \texttt{cg},
\texttt{miniweather}, \texttt{cholesky}, \texttt{lu\_decomposition}, and
\texttt{nbody\_blocked}. Table~\ref{tab:results-public-protocol} summarizes
the public experimental structure. The table reports what each experiment is
intended to establish, the number of measured repetitions used for the main
rows, and whether DVFS is enabled.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lp{0.41\linewidth}cc@{}}
\toprule
Experiment & Purpose & Repetitions & DVFS \\
\midrule
Measurement validation & Confirm that all benchmarks produce valid kernel latency and kernel-energy measurements on eight GPUs. & 1 & Off \\
Placement performance & Compare HEFT and \texttt{bandit\_scheduler} latency across the benchmark suite. & 5 & Off \\
Locality attribution & Relate placement latency changes to copy-volume reduction and non-HEFT deviation rate. & 3 & Off \\
Placement robustness & Check that the selected placement configuration is not a single-run anomaly. & 3 & Off \\
DVFS energy evaluation & Compare no-DVFS execution with \texttt{win\_dvfs} using kernel energy as the primary metric. & 1 per benchmark & On \\
DVFS mechanism analysis & Explain the energy result using kernel power and observed SM-clock exposure. & Same rows & On \\
Overhead diagnostics & Estimate the cost of compact logging and compact DVFS-control diagnostics. & 1 & Mixed \\
\bottomrule
\end{tabular}
\caption{Public experimental structure used in the results chapter.}
\label{tab:results-public-protocol}
\end{table}

Kernel latency is the primary performance metric throughout the chapter. For
DVFS, kernel energy is the primary energy metric because it integrates NVML
power samples over the benchmark kernel window. Whole-process energy is useful
as auxiliary context, but it is not used for the headline DVFS decisions in
this chapter.

\section{Measurement Validation}
\label{sec:results-measurement-validation}

Before using the benchmark data for placement or DVFS conclusions, the
measurement setup was validated on all six benchmarks. This validation checks
that both HEFT and \texttt{bandit\_scheduler} execute successfully with DVFS
disabled, that kernel timestamps are present, that NVML kernel-energy samples
are available, and that every row uses the intended eight-GPU configuration.

The validation run already shows the qualitative behavior expected from the
placement scheduler: \texttt{cg}, \texttt{cholesky}, and
\texttt{lu\_decomposition} improve substantially, whereas
\texttt{miniweather} and \texttt{nbody\_blocked} remain almost unchanged.
Because this validation uses only one measured run per row, it is not used as
the final performance evidence. The placement conclusions below use repeated
benchmark batches.

\section{Placement Performance}
\label{sec:results-placement-performance}

The main placement experiment compares HEFT against
\texttt{bandit\_scheduler} with DVFS disabled. The six benchmarks are measured
in five-run batches. Since frequency control is disabled, the latency
differences in this section are attributed to placement and scheduling
behavior rather than to clock changes.

Using the mean latency for each benchmark, \texttt{bandit\_scheduler} obtains
a six-benchmark geometric-mean speedup of \(1.076965\times\), equivalent to a
normalized runtime reduction of \(7.146\%\) relative to HEFT. Using medians,
the geometric-mean speedup is \(1.063759\times\), equivalent to a normalized
runtime reduction of \(5.994\%\). Table~\ref{tab:results-placement-main}
summarizes the per-benchmark results.

\begin{table}[t]
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}lrrrp{0.31\linewidth}@{}}
\toprule
Benchmark & Mean \(\Delta T\) & Median \(\Delta T\) & Kernel energy \(\Delta\) & Interpretation \\
\midrule
\texttt{fdtd} & \(-1.333\%\) & \(+5.629\%\) & \(-0.597\%\) & Mean latency decreases, while median latency remains noisy. \\
\texttt{cg} & \(-12.693\%\) & \(-12.468\%\) & \(-12.316\%\) & Stable latency reduction. \\
\texttt{miniweather} & \(+0.019\%\) & \(+0.040\%\) & \(-0.062\%\) & Approximately neutral. \\
\texttt{cholesky} & \(-8.638\%\) & \(-8.257\%\) & \(-2.441\%\) & Stable irregular-workload improvement. \\
\texttt{lu\_decomposition} & \(-18.539\%\) & \(-18.585\%\) & \(-17.872\%\) & Largest placement latency reduction. \\
\texttt{nbody\_blocked} & \(-0.052\%\) & \(-0.106\%\) & \(-0.580\%\) & Near neutral. \\
\bottomrule
\end{tabular}%
}
\caption{Placement-only latency and kernel-energy trace. Negative deltas are improvements relative to HEFT.}
\label{tab:results-placement-main}
\end{table}

The result is intentionally not interpreted as a uniform speedup across all
workloads. The scheduler is effective when the task graph exposes useful
copy-saving alternatives, and it stays close to HEFT when such alternatives
are absent. This behavior is consistent with the method design: the scheduler
is a HEFT-relative residual policy, not an unconstrained learned placement
rule.

\section{Locality Attribution}
\label{sec:results-locality-attribution}

The locality-attribution experiment explains the placement result by measuring
copy traffic and non-HEFT deviation rate. DVFS remains disabled. The central
question is whether the speedups in Table~\ref{tab:results-placement-main}
correspond to measurable locality improvements rather than accidental runtime
variation.

Table~\ref{tab:results-locality-attribution} shows that the largest latency
improvements align with copy-byte reductions. \texttt{cholesky} reduces median
latency by \(9.269\%\) while reducing copy bytes by \(38.220\%\), and
\texttt{lu\_decomposition} reduces median latency by \(15.891\%\) while
reducing copy bytes by \(23.905\%\). \texttt{fdtd} shows a smaller but still
consistent locality improvement. By contrast, \texttt{miniweather} and
\texttt{nbody\_blocked} have zero deviation rate and zero copy-byte reduction,
which explains their near-neutral latency behavior.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lrrrp{0.34\linewidth}@{}}
\toprule
Benchmark & Median \(\Delta T\) & Copy bytes \(\Delta\) & Deviation rate & Mechanism \\
\midrule
\texttt{fdtd} & \(-3.135\%\) & \(-12.030\%\) & 0.249902 & Modest locality improvement. \\
\texttt{miniweather} & \(-0.0066\%\) & \(0.000\%\) & 0.000000 & No useful locality opportunity. \\
\texttt{cholesky} & \(-9.269\%\) & \(-38.220\%\) & 0.281154 & Strong locality improvement. \\
\texttt{lu\_decomposition} & \(-15.891\%\) & \(-23.905\%\) & 0.619305 & Strong locality improvement. \\
\texttt{nbody\_blocked} & \(+0.019\%\) & \(0.000\%\) & 0.000000 & HEFT-like neutral behavior. \\
\bottomrule
\end{tabular}
\caption{Locality attribution for the placement scheduler. Negative latency and copy-byte deltas are improvements relative to HEFT.}
\label{tab:results-locality-attribution}
\end{table}

These results support the intended placement mechanism. When the runtime
observes a non-HEFT placement that can reduce copy traffic while keeping the
predicted finish-time penalty bounded, the residual gate accepts the
deviation. When no such opportunity exists, the scheduler naturally collapses
back to HEFT-like behavior. This explains both the strong improvements and the neutral
controls without requiring a separate benchmark-specific policy.

\section{Robustness of the Placement Configuration}
\label{sec:results-placement-sensitivity}

The placement result should not depend on a single lucky run or on a narrowly
chosen control setting. To check this, the default placement configuration was
evaluated against nearby scheduler settings that change the adaptation window
and gate-selection bias. These checks were run with DVFS disabled and used the
same benchmark methodology as the placement experiments above.

The default configuration remained the strongest aggregate choice across the
robustness check, with a four-benchmark geometric-mean speedup of
\(1.076371\times\) relative to HEFT. It also preserved the qualitative
mechanism observed in the main placement results: neutral workloads remained
close to HEFT, while the stronger linear-algebra workloads continued to show
large gains. The robustness check therefore supports using the default
\texttt{bandit\_scheduler} configuration as the thesis configuration.

\section{DVFS Energy Evaluation}
\label{sec:results-dvfs-energy}

The final DVFS experiment evaluates \texttt{win\_dvfs}. Unlike the placement
experiments above, DVFS is enabled in the \texttt{win\_dvfs} rows. The
comparison is against a no-DVFS baseline, and the primary metric is kernel
energy. The result should therefore be read as an energy-control result, not
as additional placement evidence.

Table~\ref{tab:results-dvfs-energy} reports the final six-benchmark DVFS
table. \texttt{fdtd}, \texttt{miniweather}, \texttt{cg}, and
\texttt{lu\_decomposition} show clear kernel-energy reductions.
\texttt{cholesky} also reduces kernel energy, but with a larger latency cost.
\texttt{nbody\_blocked} is near neutral, which is consistent with the
placement evidence that this workload offers little useful control
opportunity.

\begin{table}[t]
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}lrrrrrrrp{0.23\linewidth}@{}}
\toprule
Benchmark & Base ms & DVFS ms & \(\Delta T\) & Base J & DVFS J & \(\Delta E\) & \(\Delta P\) & Conclusion \\
\midrule
\texttt{fdtd} & 6296.000 & 6258.000 & \(-0.604\%\) & 3126.321 & 3006.605 & \(-3.829\%\) & \(-3.230\%\) & Clear energy reduction. \\
\texttt{miniweather} & 15127.000 & 15138.000 & \(+0.073\%\) & 4871.344 & 4721.346 & \(-3.079\%\) & \(-3.150\%\) & Clear energy reduction. \\
\texttt{cg} & 12484.000 & 12603.000 & \(+0.953\%\) & 3091.629 & 2918.909 & \(-5.587\%\) & \(-6.478\%\) & Largest energy reduction. \\
\texttt{lu\_decomposition} & 15399.100 & 15501.100 & \(+0.662\%\) & 4071.342 & 3937.518 & \(-3.287\%\) & \(-3.968\%\) & Clear energy reduction. \\
\texttt{cholesky} & 18620.000 & 19144.500 & \(+2.817\%\) & 5303.507 & 5228.698 & \(-1.411\%\) & \(-4.389\%\) & Modest energy reduction. \\
\texttt{nbody\_blocked} & 14916.379 & 14899.927 & \(-0.110\%\) & 3876.692 & 3871.392 & \(-0.137\%\) & \(-0.023\%\) & Near neutral. \\
\bottomrule
\end{tabular}%
}
\caption{Final \texttt{win\_dvfs} results. Energy and power are kernel-window NVML metrics; negative deltas are improvements relative to the no-DVFS baseline.}
\label{tab:results-dvfs-energy}
\end{table}

Across the six final rows, the geometric-mean kernel-energy ratio is
0.970962, corresponding to \(2.904\%\) lower normalized kernel energy. The
geometric-mean kernel-latency ratio is 1.006259, corresponding to a \(0.626\%\)
latency overhead. This is the main DVFS conclusion of the thesis: the
windowed, guarded DVFS layer can reduce kernel energy on the evaluated
benchmark set while keeping the aggregate latency penalty below one percent.

\section{DVFS Mechanism and Guardrail Evidence}
\label{sec:results-dvfs-mechanism}

The mechanism analysis explains why the final DVFS rows reduce energy. The
most important mechanism is lower average kernel power. In most energy-reduction rows,
the lower power is accompanied by visible exposure to a lower active SM clock.
The guardrail behavior is also visible in the near-neutral case:
\texttt{nbody\_blocked} does not show meaningful clock or power movement, so
the final outcome remains near the no-DVFS baseline rather than forcing an
energy claim.

\begin{table}[t]
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}lrrrrrrrp{0.25\linewidth}@{}}
\toprule
Benchmark & \(\Delta E\) & \(\Delta P\) & \(\Delta T\) & Base SM MHz & DVFS SM MHz & MID samples & MID time & Explanation \\
\midrule
\texttt{fdtd} & \(-3.829\%\) & \(-3.230\%\) & \(-0.604\%\) & 1931.442 & 1852.530 & 91.903\% & 1.000 & Clear clock-mediated energy reduction. \\
\texttt{miniweather} & \(-3.079\%\) & \(-3.150\%\) & \(+0.073\%\) & 2040.000 & 2028.091 & 0.000\% & 0.000 & Power drops with negligible latency cost. \\
\texttt{cg} & \(-5.587\%\) & \(-6.478\%\) & \(+0.953\%\) & 2032.581 & 1876.795 & 98.912\% & 0.333 & Largest clock-mediated energy reduction. \\
\texttt{lu\_decomposition} & \(-3.287\%\) & \(-3.968\%\) & \(+0.662\%\) & 2040.000 & 1943.170 & 58.685\% & 0.697 & Partial clock exposure lowers energy. \\
\texttt{cholesky} & \(-1.411\%\) & \(-4.389\%\) & \(+2.817\%\) & 2040.000 & 1943.222 & 58.654\% & 0.637 & Power reduction is real, latency limits net gain. \\
\texttt{nbody\_blocked} & \(-0.137\%\) & \(-0.023\%\) & \(-0.110\%\) & 2040.000 & 2040.000 & 0.000\% & 0.000 & Little DVFS opportunity. \\
\bottomrule
\end{tabular}%
}
\caption{DVFS mechanism evidence. MID samples report the fraction of kernel-window samples at the middle clock level; MID time reports the effective middle-clock time share.}
\label{tab:results-dvfs-mechanism}
\end{table}

The strongest evidence comes from \texttt{fdtd}, \texttt{cg}, and
\texttt{lu\_decomposition}. These rows combine lower kernel energy, lower
kernel power, and visible lower-clock exposure. \texttt{miniweather} is also a
useful supporting example because its latency remains essentially unchanged while
kernel power decreases. \texttt{cholesky} demonstrates the expected trade-off:
DVFS lowers power, but the latency increase absorbs part of the energy benefit.
Finally, \texttt{nbody\_blocked} acts as a neutral control. Its clock and
power metrics remain almost unchanged, and its energy result is correspondingly
near neutral.

\section{Runtime and Instrumentation Overhead}
\label{sec:results-overhead}

The overhead experiment is a diagnostic snapshot, not a replacement for the
placement or DVFS validation results above. It compares a no-log
\texttt{bandit\_scheduler} row with a compact placement-log row and a compact
\texttt{win\_dvfs} control row on \texttt{fdtd},
\texttt{lu\_decomposition}, and \texttt{nbody\_blocked}. Since this diagnostic
experiment uses one measured run per row, it should be interpreted
conservatively.

\begin{table}[t]
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}lrrrrr@{}}
\toprule
Benchmark & Log \(\Delta T\) & Log \(\Delta E\) & Process \(\Delta\) & DVFS-control \(\Delta T\) & DVFS-control \(\Delta E\) \\
\midrule
\texttt{fdtd} & \(-0.159\%\) & \(+0.324\%\) & \(-0.907\%\) & \(+0.953\%\) & \(-1.196\%\) \\
\texttt{lu\_decomposition} & \(+1.559\%\) & \(+2.072\%\) & \(+4.733\%\) & \(+9.331\%\) & \(+7.233\%\) \\
\texttt{nbody\_blocked} & \(+0.000\%\) & \(-0.624\%\) & \(+0.004\%\) & \(+4.369\%\) & \(+0.722\%\) \\
\bottomrule
\end{tabular}%
}
\caption{Single-run diagnostic overhead. Log deltas compare compact placement logging against no-log placement; DVFS-control deltas compare compact \texttt{win\_dvfs} control against no-log placement.}
\label{tab:results-overhead}
\end{table}

Compact placement logging is close to noise-scale for \texttt{fdtd} and
\texttt{nbody\_blocked}. \texttt{lu\_decomposition} shows a visible single-run
cost, so final timing claims should use the no-log validation rows. The compact
\texttt{win\_dvfs} control row is not a pure logging-only measurement because
it includes policy behavior and frequency changes. Its overhead reinforces the
need for guarded, selective DVFS rather than unconditional lower-frequency
execution.

\section{Threats to Validity}
\label{sec:results-threats}

The placement results are measured on a single eight-GPU node and may vary with
GPU topology, interconnect bandwidth, CUDA driver behavior, and benchmark
scale. The placement evaluation uses repeated runs, but some benchmarks still
show noise. \texttt{fdtd} is the clearest example: the mean and median results
point in different directions because the per-run order contains mixed
outcomes. The thesis therefore treats \texttt{fdtd} as a fragile or modest
locality case rather than as a headline placement improvement.

The locality attribution uses runtime copy metrics as mechanism evidence.
Copy-byte reductions explain the observed improvements well for
\texttt{cholesky} and \texttt{lu\_decomposition}, but they do not capture every
possible source of runtime variation, such as queueing effects, kernel launch
overheads, or overlap between computation and communication. The claim is
therefore mechanism-aligned rather than a complete causal decomposition of all
runtime effects.

The DVFS conclusion is based on kernel-window energy and kernel-window latency.
This is the right metric for the energy-control question studied here, but it
does not replace a full-system power study. The final DVFS rows show a
consistent aggregate energy reduction with small aggregate latency overhead,
yet larger run counts and additional hardware platforms would be needed before
claiming universal behavior. The diagnostic overhead results also show that
instrumentation and compact DVFS control are not free, so final timing and
energy claims should rely on the validation rows rather than on
instrumentation-heavy diagnostic rows.

\section{Results Summary}
\label{sec:results-summary}

The experimental evidence supports both layers of the proposed design. For
placement, \texttt{bandit\_scheduler} improves geometric-mean kernel latency
by \(7.146\%\) using mean latencies across six benchmarks, while the locality
analysis shows that the largest improvements coincide with large copy-byte
reductions. The neutral controls show the complementary behavior: when no
copy-saving opportunity is present, the scheduler stays close to HEFT rather
than forcing arbitrary deviations. The sensitivity analysis confirms that the
selected default configuration is the best cross-workload choice among the
tested alternatives.

For DVFS, \texttt{win\_dvfs} reduces geometric-mean kernel energy by
\(2.904\%\) with \(0.626\%\) geometric-mean kernel-latency overhead. The
mechanism evidence shows that the largest energy reductions are associated with
lower kernel average power and, in most cases, visible exposure to lower SM
clock levels. The final result is therefore not a universal claim that every
benchmark benefits from DVFS. Instead, it is a controlled result showing that a
guarded proposal--commit DVFS layer can extract energy savings from several
CUDASTF workloads while keeping the aggregate latency cost small and leaving
low-opportunity workloads near the baseline.
